import React from "react";
import styled from "styled-components";

const HeaderComponent = () => {
  return <Header>Dividend Assistant</Header>;
};

const Header = styled.h2`
  grid-area: header;
  margin-top: 20px;
`;
export default HeaderComponent;
